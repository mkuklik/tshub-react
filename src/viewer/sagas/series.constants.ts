export const SeriesKind = Object.freeze({
  data: "data",
  expr: "expr",
});

export const SeriesStatus = Object.freeze({
  RESOLVING: "resolving",
  SUCCESS: "success",
  ERROR: "error",
});

export enum TSeriesKind {
  data = "data",
  expr = "expr",
}

export enum TSeriesStatus {
  RESOLVING = "resolving",
  SUCCESS = "success",
  ERROR = "error",
}
